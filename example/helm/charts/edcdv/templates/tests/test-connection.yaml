apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "edcdv.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "edcdv.name" . }}
    helm.sh/chart: {{ include "edcdv.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "edcdv.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
